# Portable library integration

Serve this repository as static files and retain its relative directory structure. `index.html` is the document entry point; each other root HTML file is a standalone collection view. Original documents and images should keep their names and bytes so the catalogs and source hashes continue to resolve.

The public package contains no private platform code or deployment maps. Bind its UI and catalogs to the host's existing shell, authorization, document storage and tool contracts. Keep public source discovery, official document evidence, task instructions and synthetic test scenarios in separate collections.

The agent skills module exports a mount API and a React adapter under `10-Agent-Skills-Library/integration/`. Preserve its source notices, stable IDs and schema. Model execution, runtime permissions, matter scope, document selection and saving to shared storage must be provided by the host. Local task briefs and JSON exports are working frontend features; they do not run an agent.

The settlement module under `07-Settlement-References/catalog/` exposes reusable pure filtering/export functions and a standalone frontend. Preserve full publisher text and separate dated source reviews. Keep source credit in UI and exports. Do not turn reported dates into calendar events without checking the operative source and cutoff.

The five Python utilities under `11-Platform-Corpus/tools/` perform deterministic source normalization, scan planning, retry reconciliation, literal quotation checking and safe CSV export. They do not establish proposition support or legal accuracy. Run their included tests after adapting the contracts.

For a static HTTP deployment, use `/index.html` as the entry URL. No deployment credentials, service configuration or active automation is included in this repository. Use a host-appropriate authenticated document route if the library is combined with private matter material.
